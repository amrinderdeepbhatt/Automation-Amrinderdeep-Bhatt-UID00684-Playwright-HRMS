[project]
name = "amrinderdeep-playwright"
version = "0.1.0"
description = ""
authors = [
    {name = "amrinderdeepbhatt",email = "amrinderdeep.bhatt@joshtechnologygroup.com"}
]
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pytest (>=9.0.3,<10.0.0)",
    "pytest-bdd (>=8.1.0,<9.0.0)",
    "pytest-playwright (>=0.7.2,<0.8.0)",
    "playwright (>=1.58.0,<2.0.0)",
    "python-dotenv (>=1.2.2,<2.0.0)",
    "pyyaml (>=6.0.3,<7.0.0)",
    "requests (>=2.33.1,<3.0.0)",
    "faker (>=40.13.0,<41.0.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
